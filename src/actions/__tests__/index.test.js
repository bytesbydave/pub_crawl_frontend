import {
  signIn,
  signOut,
  createLocation,
  fetchLocation,
  fetchLocations,
  deleteLocation,
  editLocation,
  createCrawls,
  fetchCrawls,
  fetchCrawlss,
  deleteCrawls,
  editCrawls
} from '../../actions';
import {
  SIGN_IN,
  SIGN_OUT,
  CREATE_CRAWL,
  FETCH_CRAWLS,
  FETCH_CRAWL,
  DELETE_CRAWL,
  EDIT_CRAWL,
  CREATE_LOCATION,
  FETCH_LOCATIONS,
  FETCH_LOCATION,
  DELETE_LOCATION
} from '../../actions/types';

describe('createLocation', () => {
  // it('has the correct type', () => {
  //   const action = createLocation();

  //   expect(action.type).toEqual(CREATE_LOCATION);
  // });

  // it('has the correct payload', () => {
  //   const action = createLocation('1', 'Rudys bar');

  //   expect(action.payload).toEqual('New Comment');
  // });
});